# Sample Output

Small committed snapshot from `run_20260503_130136_final_submission`. It is intentionally tiny so reviewers can inspect the artifact shape and run artifact-backed queries without downloading the private full dataset.

Try:

```bash
.venv/bin/cartsy-dedupe search "wella" --run outputs/sample --backend artifacts --limit 5
```
